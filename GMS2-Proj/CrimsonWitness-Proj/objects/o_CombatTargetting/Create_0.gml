event_inherited();

hasfocus = false;

// Add elements
for(var i = 0; i < instance_number(o_Enemy); i++){menuitems[0,i] = 0;} // TODO: Set the element to the enemy's name and display info about it.

// Display
cursorX = 1280/2;
cursorY = 720/2;